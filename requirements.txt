# Core dependencies for vLLM multi-GPU backend
# Python 3.8+ required

# vLLM and ML frameworks
vllm>=0.6.0
torch>=2.1.0
transformers>=4.45.0
accelerate>=0.25.0
safetensors>=0.4.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Memory optimizations 
einops>=0.8.0
# flash-attn>=2.5.0  # Install via install_deps.sh script

# API Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
jinja2>=3.1.2

# GPU Monitoring
pynvml>=11.5.0
psutil>=5.9.0

# Model management
huggingface-hub>=0.19.0
tokenizers>=0.15.0

# CLI interface
click>=8.1.0
rich>=13.0.0

# Async support
aiohttp>=3.9.0
httpx>=0.25.0

# Optional optimizations (install manually if needed)
# flash-attn>=2.5.0  # For FlashAttention support - significant memory savings
# einops>=0.8.0  # Required by flash-attn

# Optional optimizations (install separately if needed)
# xformers>=0.0.23  # For memory efficient attention

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0